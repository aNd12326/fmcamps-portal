---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<div class="w-full h-screen text-white">
    <!-- <div
        class="w-[1000px] mx-auto flex flex-col justify-center h-full text-center"
    >
        <h1 class="text-7xl font-semibold py-6 text-transform-gradient">
            {t("hero.title")}
        </h1>
        <h2 class="text-5xl font-semibold py-6">
            {t("hero.subtitle")}
        </h2>
        <p class="text-2xl">
            {t("hero.lastp")}
        </p>
    </div> -->
    <div
        class="w-full h-full mx-auto text-center flex flex-col items-center justify-center gap-y-3"
    >
        <h1 class="text-transform-gradient">
            {t("hero.title")}
        </h1>

        <h2>
            {t("hero.subtitle")}
        </h2>

        <p>
            {t("hero.lastp")}
        </p>
    </div>
</div>
